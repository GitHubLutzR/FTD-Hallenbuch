<?php
require_once(__DIR__ . '/../config.php');
session_start();

// Zugriffsschutz
if (!isset($_SESSION['user'])) {
    echo "<p>⛔ Kein Zugriff. Bitte <a href='login.php'>einloggen</a>.</p>";
    exit;
}

$id = $_GET['id'] ?? null;
if ($id) {
    $stmt = $pdo->prepare("DELETE FROM hb_users WHERE id = ?");
    $stmt->execute([$id]);
    echo "Benutzer gelöscht.";
}
?>

