<?php
require_once 'init.php';
check_admin();

$id = $_GET['id'] ?? null;
if ($id) {
    $stmt = $pdo->prepare("DELETE FROM hb_users WHERE id = ?");
    $stmt->execute([$id]);
    echo "Benutzer gelöscht.";
}
?>

